class Links:
    HOST = "http://localhost:3000"
    LOGIN = f"{HOST}/login"
    REGISTER = f"{HOST}/register"
    MAIN = f"{HOST}/main"
    WORK_PROGRAM = f"{HOST}/work_program"
    UPLOAD_PROGRAMS = f"{HOST}/upload_programs"
    GRAPH = f"{HOST}/graph"